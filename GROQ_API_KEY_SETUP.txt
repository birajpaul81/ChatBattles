GROQ API KEY SETUP
==================

Add this line to your .env.local file:

GROQ_API_KEY=your_groq_api_key_here

Then restart your development server:
npm run dev

That's it! GPT-5 will now use Groq's openai/gpt-oss-120b model.
